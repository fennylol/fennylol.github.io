---
import type { CollectionEntry } from 'astro:content';
import FormattedDate from './FormattedDate.astro';

interface Props {
  post: CollectionEntry<'blog'>;
}
const { post } = Astro.props;
const { data, id } = post;
---

<article class="post-preview">
  <div class="head">
    <h3><a href={`/blog/${id}`}>{data.title}</a></h3>
    <p class="date"><FormattedDate date={data.date} /></p>
  </div>
  {data.subtitle && <p class="subtitle">{data.subtitle}</p>}
  <p class="summary">{data.summary}</p>
</article>

<style>
  /*
   * Visual weight runs title → subtitle → summary → date, in both size and
   * brightness, even though the date sits top-right. Sizes step down through
   * the type scale; colours step down the --color-text/subtle/muted/faint
   * ramp in tokens.css.
   */
  .head {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--space-3xs) var(--space-s);
  }

  .post-preview h3 {
    font-size: var(--size-step-1);
    color: var(--color-text);
  }
  .post-preview h3 a {
    color: inherit;
    text-decoration: none;
  }
  .post-preview h3 a:hover {
    color: var(--color-link);
    text-decoration: underline;
  }

  .date {
    font-size: var(--size-step--2);
    color: var(--color-faint);
    white-space: nowrap;
  }

  .subtitle {
    font-size: var(--size-step-0);
    color: var(--color-subtle);
    line-height: 1.35;
    margin-top: var(--space-3xs);
  }

  .summary {
    font-size: var(--size-step--1);
    color: var(--color-muted);
    margin-top: var(--space-2xs);
  }
</style>
